croc.View.define('croc.cmp.tooltip.Pointer.View', {
    members: {
        /**
         * Возвращает элемент точки крепления. Например, стрелка тултипа.
         * @returns {jQuery}
         */
        getJointEl: function() {
            return null;
        }
    }
});